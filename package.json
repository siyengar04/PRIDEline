{
  "dependencies": {
    "axios": "^1.4.0",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "scripts": {
    "start": "node server.js",
    "build": "cd client && npm install && npm run build && cd .. && npm install"
  }
}
